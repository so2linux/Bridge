# Как запускать и перезапускать Bridge

## Перезапуск бэкенда

1. В терминале, где запущен бэкенд, нажми **Ctrl+C** (остановить сервер).
2. Выполни команды:

**Windows (cmd или PowerShell из папки проекта):**
```cmd
cd backend
venv\Scripts\activate
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

**Windows (если ты не в папке проекта):**
```cmd
cd C:\Users\ЗС\Desktop\Bridge\backend
venv\Scripts\activate
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

**Linux / macOS:**
```bash
cd backend
source venv/bin/activate
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

После перезапуска миграции применятся заново (если были изменения в БД). При первом запуске в каталог подарков добавятся 5 подарков (☕ Кофе, 🧱 Кирпич, 🌉 Мост, 💎 Кристалл, 🚀 Ракета). Дополнительно можно выполнить: `python -m scripts.seed_gifts` из папки `backend` — обновит названия и цены подарков.

Голосовые сообщения сохраняются в `static/voice/`.

---

## Перезапуск фронта

В терминале фронта: **Ctrl+C**, затем:
```bash
cd frontend
npm run dev
```
Фронт откроется на **http://localhost:3000**; запросы `/api` идут на бэкенд через прокси.

---

## Если чат не открывается

1. **Запускай оба сервера:** сначала бэкенд (порт 8000), потом фронт (`npm run dev`, порт 3000).
2. **Открывай приложение по адресу:** `http://localhost:3000` или `http://127.0.0.1:3000` (не `file://` и не другой порт без прокси).
3. **Фронт с другого устройства/порта:** в корне фронта создай `.env` с `VITE_API_URL=http://IP:8000` (подставь IP машины с бэкендом). В папке `backend` создай/дополни `.env`: `CORS_ORIGINS=http://IP:3000,http://localhost:3000,...` (добавь свой origin), затем перезапусти бэкенд.
4. **Ошибка в интерфейсе:** смотри текст под кнопкой «К списку чатов» — там будет код (403/404) и сообщение; при сетевой ошибке — «Нет связи с сервером».

---

## Добавление аккаунта

1. Войди в приложение под любым аккаунтом.
2. В сайдбаре нажми **«Добавить аккаунт»** (откроется страница входа с подсказкой).
3. Нажми **«Регистрация»** — откроется регистрация **без выхода** из меню добавления аккаунта.
4. Заполни форму и нажми **«Зарегистрироваться»** — новый аккаунт добавится в список (до 3 аккаунтов), ты останешься в приложении под новым аккаунтом.

Если аккаунт уже есть — на странице входа (после «Добавить аккаунт») введи email и пароль и нажми **«Войти»** — этот аккаунт тоже добавится в меню.

---

## Верификация по почте (позже)

Идея: при регистрации отправлять на email код, пользователь вводит код и подтверждает. Реализацию можно добавить позже (SMTP или сервис писем, таблица кодов в БД, эндпоинт проверки кода).
